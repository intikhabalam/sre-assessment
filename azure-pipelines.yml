# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Pay-As-You-Go(5b58a2c2-c9bd-4698-9dfa-6e172835c0b9)'
    scriptType: 'batch'
    scriptLocation: 'scriptPath'
    scriptPath: 'Infrastructure/powershell/tf-sa.sh'